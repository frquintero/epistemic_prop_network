"""
Development helper removed.

This script was a temporary dry-run helper used during development to
verify LLM prompt rendering. It has been removed from active use to
keep the repository clean. If you need the helper again, check the
git history or re-create a new dev script under `scripts/`.
"""
